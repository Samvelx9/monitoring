# config file version
apiVersion: 1

# List of templates to import or update
templates:
  # <int> organization ID, default = 1
  - orgId: 1
    # <string, required> name of the template, must be unique
    name: title
    # <string, required> content of the template
    template: |
      {{ define "title" }}
        {{ if gt (len .Alerts) 0 }}
          {{ .CommonLabels.alertname | toUpper }} ALERT
        {{ end }}
      {{ end }}